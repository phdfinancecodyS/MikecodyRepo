[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r ask-anyway/cce-backend/requirements.txt"
]

[start]
cmd = "cd ask-anyway/cce-backend && uvicorn src.app:app --host 0.0.0.0 --port $PORT"
