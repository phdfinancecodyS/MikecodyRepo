# Netlify deployment config for Ask Anyway (Next.js App Router)
# Docs: https://docs.netlify.com/integrations/frameworks/next-js/overview/

[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
