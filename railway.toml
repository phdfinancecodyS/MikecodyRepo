[build]
builder = "nixpacks"

[deploy]
startCommand = "cd ask-anyway/cce-backend && /app/venv/bin/uvicorn src.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
